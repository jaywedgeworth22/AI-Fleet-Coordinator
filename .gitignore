fleet-sentry-monitor/.env
fleet-sentry-monitor/state.json

__pycache__/
*.pyc


.DS_Store

# Local Python venvs (e.g. the fleet_rag experiments venv)
.venv/

# fleet_rag experiments: local corpus cache (read-only dump of fleet-agents payload text,
# not code -- rebuild with `python3 -m fleet_rag.experiments.corpus_cache --rebuild`)
scripts/fleet_rag/experiments/.cache/

# Generated per-seat zip upload packs (SKILL.md trees are tracked)
docs/fleet-skills/by-seat/**/*.zip

# Local fleet health recover env files
scripts/host/fleet-health-recover.d/*.env
*.db

# Wrangler local dev cache (start.jays.services Worker)
scripts/safari-start/.wrangler/
